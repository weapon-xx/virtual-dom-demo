"use strict";

(function(exports) {
  // 差异类型
  const REPLACE = 0  // 替换节点类型
  const REORDER = 1  // 移动、删除、新增子节点
  const PROPS = 2    // 修改节点属性
  const TEXT = 3     // 修改文本节点



  exports.patch = {}
  patch.REPLACE = REPLACE
  patch.REORDER = REORDER
  patch.PROPS = PROPS
  patch.TEXT = TEXT

})(window)
